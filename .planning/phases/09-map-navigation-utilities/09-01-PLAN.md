# Plan: 09-01 — Geolocation & Map Control

Add the ability to center the map programmatically and implement the "Locate Me" feature.

## Proposed Changes

### `store/useRouteStore.ts`
- Add `mapCenter: [number, number] | null` and `mapZoom: number | null` to state.
- Add `flyTo: (lat: number, lon: number, zoom?: number) => void` action.

### `components/MapComponent.tsx`
- Add `useEffect` that listens to `mapCenter`.
- Use `map.flyTo(mapCenter, mapZoom)` when it changes.

### `components/Sidebar.tsx`
- Add a "Locate Me" button in the header section.
- Implement geolocation logic using `navigator.geolocation`.
- Handle permission errors with a simple `alert` or console warning for now.

## Verification Plan
- Click "Locate Me" and ensure map moves to current location.
- Verify map doesn't loop forever after flying.
