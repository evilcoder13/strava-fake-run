# Plan: 10-02 — URL Syncing & UI

Integrate URL state into the Next.js app and add the "Share" control.

## Proposed Changes

### `components/UrlSync.tsx` (New)
- Use `useEffect` + `window.history.replaceState` to update the URL when the store changes.
- Handle initial mount: check `?s={data}` and call `loadFromState`.
- Use a flag to prevent update loop (e.g., `isInitialLoading`).

### `app/layout.tsx` (or `page.tsx`)
- Render `<UrlSync />` at the top level.

### `components/Sidebar.tsx`
- Add "Share Link" button with a "Copied!" feedback state.
- Use Lucide `Share2` icon.

## Verification Plan
- Share a link to a friend (simulated).
- Verify that refreshing the page maintains all waypoints and the generated activity charts (if possible).
