---
phase: 01
wave: 1
depends_on: []
files_modified: []
autonomous: true
---

# Plan 01-01: Bootstrap Next.js & UI Scaffolding

## Goal
Initialize the Next.js 15 App Router project with Tailwind CSS, remove boilerplate, and setup the requested UI dependencies (lucide-react, zustand, dnd-kit) before doing map integrations.

## Requirements
- MAP-01, MAP-02, MAP-03 (Foundation only)

## Verification
- [ ] `package.json` contains `next`, `tailwindcss`, `lucide-react`, `zustand`, `@dnd-kit/core`, `@dnd-kit/sortable`
- [ ] `npm run build` succeeds
- [ ] `app/page.tsx` boilerplate is cleared

## Tasks

```xml
<task>
  <action>
    Run `npx -y create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*" --use-npm` to bootstrap the Next.js project. Ensure no boilerplate remains in `app/page.tsx`.
  </action>
  <read_first>
    - .planning/PROJECT.md
  </read_first>
  <acceptance_criteria>
    - `ls package.json` succeeds and `next` is installed.
    - `grep "main" app/page.tsx` has been replaced with a clean functional component.
  </acceptance_criteria>
</task>

<task>
  <action>
    Run `npm install lucide-react zustand @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities` to install additional required libraries for the state and UI features.
  </action>
  <read_first>
    - package.json
  </read_first>
  <acceptance_criteria>
    - `grep "lucide-react" package.json` succeeds.
    - `grep "zustand" package.json` succeeds.
  </acceptance_criteria>
</task>

<task>
  <action>
    Modify `tailwind.config.ts` (if applicable in the generated Next.js version, it might be `globals.css` if Next.js 15 Tailwind V4 is used) to include the Strava Orange brand color `#FC4C02` as configured in `01-UI-SPEC.md`. Update `app/globals.css` to clean out standard Next.js dark mode invert logic if it gets in the way of a clean design.
  </action>
  <read_first>
    - .planning/phases/01-project-setup-core-map/01-UI-SPEC.md
    - tailwind.config.ts / app/globals.css
  </read_first>
  <acceptance_criteria>
    - Grep for `FC4C02` in tailwind config or globals.css succeeds.
  </acceptance_criteria>
</task>
```
