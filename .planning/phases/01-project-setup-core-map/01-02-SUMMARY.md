# 01-02 Summary

## What was built
- Installed `leaflet`, `react-leaflet`, and type definitions (using v4 for react-18 compatibility).
- Added Leaflet CSS to `app/globals.css`.
- Created `store/useRouteStore.ts` utilizing Zustand for managing waypoint state (`id`, `lat`, `lng`).
- Added actions for `addWaypoint`, `removeWaypoint`, `reorderWaypoints`, and `moveWaypoint`.

## Next Steps
- Build UI layout and map components.
