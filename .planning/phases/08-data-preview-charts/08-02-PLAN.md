# Plan: 08-02 — Sidebar integration & UI Polish

Embed the charts into the Sidebar with a collapsible "Preview" section.

## Proposed Changes

### `components/Sidebar.tsx`
- Import `ActivityCharts` using `next/dynamic` to ensure client-side rendering.
- Add a collapsible section (Details/Summary or simple state) below the activity generation button.
- Pass `generatedActivity` to the chart component.
- Display "Generate an activity to see charts" placeholder if empty.

### Styling
- Ensure charts have a fixed height (e.g., 200px) that fits nicely in the sidebar.
- Hide Y axis labels if too narrow, or use tiny fonts.

## Verification Plan
- Click "Generate Activity" and verify charts appear automatically.
- Check tooltip behavior — it should show "Pace: 5:30/km" type formatting.
