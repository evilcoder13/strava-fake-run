# Plan: 08-01 — Chart Installation & Data Wiring

Install Recharts and update the activity generation pipeline to include charting data.

## Proposed Changes

### Dependencies
- Run `npm install recharts`

### `lib/types/activity.ts`
- Add `paceMinKm: number` to `ActivityPoint` interface.

### `store/useRouteStore.ts`
- Update `generateActivity` to include `paceMinKm` in the generated points.

### `components/ActivityCharts.tsx` (New)
- Create a client-side component using `ResponsiveContainer`, `LineChart`, `XAxis`, `YAxis`, `Tooltip`.
- Implement `PaceChart` and `HRChart` sub-components.
- Use Strava-orange for HR (#FC4C02) and a contrast color for Pace (e.g., #22c55e).

## Verification Plan
- Run `npm install` and verify `package.json`.
- Check `npx tsc` for type errors.
